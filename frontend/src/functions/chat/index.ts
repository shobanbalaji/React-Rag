import axios from "axios";

const getAllChats = async (userId: string) => {
    try {
        const uId = userId;
    } catch (error) {
        console.error(error)
    }
}


export {
    getAllChats
}